---
summary: "Source-of-truth paths, symlinks, runtime folders, and machine-specific local data."
read_when:
  - Debugging broken symlinks.
  - Changing install targets, runtime paths, Codex config, or tool config layout.
---

# Path Layout

## Source of truth

```text
<repo>/        # git repo
~/.agents/    # local hub of symlinks into the repo
```

The two-level layout lets each person move the repo later and only update `~/.agents/*`.

## Repo paths

| Path | Purpose |
|---|---|
| `AGENTS.md` | Shared instructions for agents |
| `agents/` | Shared subagents |
| `skills/` | Versioned skills source tree |
| `.runtime/skills` | Generated flat skills runtime, ignored by Git |
| `claude/` | Claude settings, MCP template, statusline |
| `codex/` | Codex config, rules and agents |
| `docs/` | Durable docs |
| `scripts/` | Sync and validation helpers |
| `.githooks/` | Optional local Git hooks |

## `~/.agents` symlinks

| Symlink | Target |
|---|---|
| `~/.agents/AGENTS.md` | `<repo>/AGENTS.md` |
| `~/.agents/agents` | `<repo>/agents` |
| `~/.agents/skills` | `<repo>/.runtime/skills` |
| `~/.agents/codex` | `<repo>/codex` |
| `~/.agents/claude` | `<repo>/claude` |

## Tool symlinks

| Tool | Symlink | Target |
|---|---|---|
| Claude | `~/.claude/settings.json` | `~/.agents/claude/settings.json` |
| Claude | `~/.claude/CLAUDE.md` | `~/.agents/AGENTS.md` |
| Claude | `~/.claude/agents` | `~/.agents/agents` |
| Claude | `~/.claude/skills` | `~/.agents/skills` |
| Codex | `~/.codex/config.toml` | `~/.agents/codex/config.toml` |
| Codex | `~/.codex/rules/default.rules` | `~/.agents/codex/rules/default.rules` |
| Codex | `~/.codex/AGENTS.md` | `~/.agents/AGENTS.md` |
| Codex | `~/.codex/agents` | `~/.agents/codex/agents` |
| Codex | `~/.codex/skills/<skill>` | `~/.agents/skills/<skill>` |

## Local-only files

| File | Why local |
|---|---|
| `codex/config.toml` | Codex config used as the final config |
| `claude/claude.json` | Sanitized Claude state/config baseline |
| `.runtime/` | Generated by `skills-cli` |
| `codex/plugins-cache` | Local plugin cache |
| `codex/plugin-sources` | Local plugin source mirror |

Pets are intentionally not part of this repo.
