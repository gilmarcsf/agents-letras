---
name: ui
description: "Explore, build, and refine UI."
---

# UI

## Codex Instructions

Use the `uidotsh://ui` resource

## Claude Instructions

This skill is managed remotely. Use `mcp__uidotsh__uidotsh_fetch` to fetch `uidotsh://ui` from the MCP server, treat the fetched content as this skill's `SKILL.md` instructions, and follow any additional `uidotsh://...` references it points to.
